services:
  discord-bot:
    build: .
    container_name: fumottsbot
    restart: unless-stopped
    user: root
    env_file:
      - .env
    volumes:
      - .:/app
      - /app/node_modules
    command: npm start